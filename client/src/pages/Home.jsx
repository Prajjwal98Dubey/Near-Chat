import MainContainer from "../components/MainContainer";

const Home = () => {
  return (
    <div className="font-inter font-bold w-full min-h-screen bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50">
      <MainContainer />
    </div>
  );
};

export default Home;
